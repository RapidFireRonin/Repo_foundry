# Next Steps

1. Connect reconcile apply mode to GitHub CLI or GitHub App credentials.
2. Add live GitHub Actions and PR polling.
3. Implement the PR status collector that emits `schemas/pr-status-snapshot.schema.json` artifacts.
4. Add branch protection verification against the GitHub API.
5. Expand dashboard editing for blueprints.
