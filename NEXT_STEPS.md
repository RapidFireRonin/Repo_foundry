# Next Steps

1. Add PR/check status snapshots for open PRs, including mergeability, changed-file count, dependency-file changes, risky paths, and required-check results.
2. Surface the latest completion snapshot in the dashboard PR panel with `merge-ready`, `waiting`, `needs-repair`, `superseded`, and `blocked-by-policy` states.
3. Add dashboard-native `Direct the Agents` prompt box with priority, details, API submit, audit log entry, and immediate queue refresh.
4. Connect reconcile apply mode to GitHub CLI or GitHub App credentials.
5. Add live GitHub Actions and PR polling.
6. Add branch protection verification against the GitHub API.
7. Expand dashboard editing for blueprints.