# Completion Loop Readme

This branch adds the first Repo Foundry PR completion surface.

Files added:
- policy JSON
- Python sweeper
- scheduled report workflow
- pytest coverage

The default workflow is observation-only. The script itself can be used by a trusted runner for live completion after dashboard visibility is in place.
